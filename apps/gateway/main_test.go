package main

import (
	"testing"
)

// TODO(lidor): need to add checks for all REST API endpoints
func TestHello(t *testing.T) {
	// result := Hello("world")
	// if result != "Hello world" {
	// 	t.Error("Expected Hello to append 'world'")
	// }
}
